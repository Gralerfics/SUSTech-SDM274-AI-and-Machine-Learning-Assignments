# SDM274 Assignment 05 - Multilayer Perceptron












1、写一个 mlp，任意结构；正反向传播。
2、实现 MBGD 和 SGD；综合进训练模型的过程。
3、k-fold CV 评估模型；可以验证不同超参对模型 acc 的影响。
4、选一个复杂非线性单输入单输出函数；加噪声生成数据集；尝试不同结构的 mlp 拟合；用 k-fold CV 评估各模型；演示结果和分析。
5、创建和 ppt 类似的数据集；用不同结构的 mlp 分类；用 k-fold CV 验证并选出最好的；演示分类结果并评估 acc、recall、precision、f1。

代码注释、结构；
报告包括方法、实验、结论；
报告包括数据集可视化、模型表现和其他相关信息。
